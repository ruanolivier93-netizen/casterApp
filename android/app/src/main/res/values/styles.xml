<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <style name="CastControlsTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="mediaRouteTheme">@style/MediaRouterTheme.RLCaster</item>
        <item name="castMiniControllerStyle">@style/RLCasterMiniController</item>
        <item name="castExpandedControllerStyle">@style/RLCasterExpandedController</item>
    </style>

    <style name="MediaRouterTheme.RLCaster" parent="Theme.MediaRouter">
        <item name="mediaRouteButtonStyle">@style/Widget.RLCaster.MediaRouteButton</item>
    </style>

    <style name="Widget.RLCaster.MediaRouteButton" parent="Widget.MediaRouter.Light.MediaRouteButton">
        <item name="mediaRouteButtonTint">#FFFFFFFF</item>
    </style>

    <style name="RLCasterMiniController" parent="CastMiniController">
        <item name="castControlButtons">@array/cast_mini_controller_control_buttons</item>
        <item name="castBackground">#FF111827</item>
        <item name="castTitleTextAppearance">@style/TextAppearance.AppCompat.Subhead</item>
        <item name="castSubtitleTextAppearance">@style/TextAppearance.AppCompat.Caption</item>
        <item name="castShowImageThumbnail">true</item>
    </style>

    <style name="RLCasterExpandedController" parent="CastExpandedController">
        <item name="castControlButtons">@array/cast_expanded_controller_control_buttons</item>
        <item name="castButtonColor">#FFFFFFFF</item>
    </style>
</resources>
